<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lslm">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FindPassword">
            <intent-filter>
                <action android:name="android.intent.action.FindPassword"></action>
            </intent-filter>
        </activity>

        <activity android:name=".ChangePassword">
            <intent-filter>
                <action android:name="android.intent.action.ChangePassword"></action>
            </intent-filter>
        </activity>

        <activity android:name=".LoginPhone">
            <intent-filter>
                <action android:name="android.intent.action.LoginPhone"></action>
            </intent-filter>
        </activity>
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="android.intent.action.Register"></action>
            </intent-filter>
        </activity>

        <activity android:name=".PageMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.PageMainActivity"></action>
            </intent-filter>
        </activity>
    </application>

</manifest>